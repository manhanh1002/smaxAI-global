I have updated the plan to reflect your preference for a light background on the login page.

## 1. Clean up & Standardize Login Screens
**Target Files:** `smax-frontend/src/pages/Login.tsx`, `smax-frontend/src/pages/admin/AdminLogin.tsx`
- **Background:** Set background to a **light gray-white** (e.g., `bg-gray-50`) as requested, ensuring a clean, modern look.
- **Remove Redundant Elements:**
  - Remove "Smax AI" and "Autonomous Customer Operator" text (Logo is sufficient).
  - Remove "Status: Configured" and "URL" debug text.
- **Apply Design System:**
  - Update input fields to use **Neutral** borders and **Primary** focus rings.
  - Update the "Sign In" button to use **Primary/Accent** color tokens.
  - Ensure the login card uses the correct **Shadow** and **Border Radius** (12px) to stand out against the light background.

## 2. Update Core Components to Design Tokens
**Target Files:** `Button.tsx`, `Card.tsx`, `Modal.tsx`
- **Button:** Fix height (4px grid aligned) and set border radius to `8px`.
- **Card:** Set border radius to `12px` and apply standard **Card Shadow**.
- **Modal:** Set border radius to `8px` and increase padding to `24px`.

## 3. Fix Dashboard & Global Styling
**Target Files:** `Dashboard.tsx`, `tailwind.config.js`
- **Dashboard:** Replace off-brand `indigo-purple` gradients with **Primary/Accent** brand gradients.
- **Tailwind Config:** Ensure radius and color tokens map correctly to the design system.